# dict(id=row[0], descriere=row[1], valoare=row[2])
# {'id': row[0], 'descriere': row[1], 'valoare': row[2]}

for letter in 'Python':
   if letter == 'h':
      continue
   print 'Current Letter :', letter